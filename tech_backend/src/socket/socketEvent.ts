import { Socket } from "socket.io";

const registerSocketEvents = (socket: Socket) => {
  //Event for when user is created
  //Event for when user is deleted
  //Event for when user is updated
  //Event for when note is created
  //Event for when note is deleted
  //Event for when note is updated
};

export default registerSocketEvents;

// doubt about this  need
